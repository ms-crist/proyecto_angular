/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FechaPipe } from './fecha.pipe';

describe('Pipe: Fechae', () => {
  it('create an instance', () => {
    let pipe = new FechaPipe();
    expect(pipe).toBeTruthy();
  });
});
